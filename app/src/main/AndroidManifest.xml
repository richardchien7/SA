<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name1"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShowReservation"></activity>
        <activity android:name=".hospital_information" />
        <activity android:name=".register" />
        <activity android:name=".res_time" />
        <activity android:name=".confirm" />
        <activity android:name=".sch" />
        <activity android:name=".choose_division" />
        <activity android:name=".personalPage" />
        <activity android:name=".medicineRemind"/>
        <activity android:name=".reviewRemind"/>
        <activity android:name=".conditionRecord"/>
        <activity android:name=".personalDocument"/>
        <activity android:name=".fragment.ContainerActivity" />
        <!-- <activity android:name=".home" /> -->
        <activity android:name=".home">
        </activity>
    </application>

</manifest>